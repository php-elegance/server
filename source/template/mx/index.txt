<?php

namespace Elegance;

chdir(__DIR__);

date_default_timezone_set('America/Sao_Paulo');

require './vendor/autoload.php';

Router::middleware(['elegance.cros', 'elegance.json']);

Router::add('assets...', fn () => Assets::send('assets/', ...Request::route()));
Router::add('favicon.ico', fn () => Assets::send('#elegance-server/assets/favicon.ico'));

Router::add('', '#Wellcome to Elegance PHP');

Router::import();

Router::solve();
